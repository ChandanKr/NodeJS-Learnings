//! Connecting NodeJS with MongoDB | Mongoose + Express

// Step 1: install mongoose dependency: "npm i mongoose"
// Step 2: require mongoose in index.js file: "const mongoose = require("mongoose");"
//         \_______ Schema - Define the structure.
//                  \_______ Schema Model.
//                  \_______ Using Model, we do CRUD operations.

// Step 3: Make connection(NodeJS + MongoDB) : "mongoose.connect("url/give-your-db-name");" 
//         e.g.  =>  mongoose.connect("mongodb://127.0.0.1:27017/my-first-db");
//*         [Note: it will return a promise, view detailed code in index.js file]

// Step 4: After requiring, Create Schema.

// Step 5: Create Schema Model.

// Step 6: Start CRUD Operations.


